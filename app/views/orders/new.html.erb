<%= form_with model: [ @course, @order ]  do |f| %>
  <%= f.label :pay_type, t('.pay_type') %>
  <%= f.collection_select :pay_type, PayType.all, :token, :name %>
  <%= f.submit t(".submit") %>
<% end %>
