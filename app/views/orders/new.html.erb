<%= form_with url: orders_path(course: @course) do |f| %>
  <%= f.label :pay_type, t('.pay_type') %>
  <%= f.collection_select :pay_type, PayType.all, :name, :name, { include_blank: true } %>
  <%= f.submit t(".submit") %>
<% end %>
